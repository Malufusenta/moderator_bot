"""
Нормализация текста перед поиском стоп-слов и перед хешированием объявлений.

Задачи:
  - Привести к нижнему регистру.
  - Схлопнуть/удалить пробелы и пунктуацию внутри слов (борьба с «про дам», «про-дам»).
  - Нейтрализовать частые гомоглифы: латиница → кириллица, «0» → «о» и т.п.
  - Убрать эмодзи и нечитаемые Unicode-символы.

# TODO (раздел 6.2 ТЗ): реализовать normalize(text: str) -> str.
#   Использовать re.sub для схлопывания лишних пробелов и пунктуации,
#   таблицу str.maketrans для гомоглифов, unicodedata для эмодзи.
"""

from __future__ import annotations


def normalize(text: str) -> str:
    """Нормализовать текст для поиска стоп-слов и сравнения объявлений.

    # TODO (раздел 6.2): привести к нижнему регистру, убрать лишние символы,
    #   заменить гомоглифы, схлопнуть пробелы.
    """
    raise NotImplementedError
