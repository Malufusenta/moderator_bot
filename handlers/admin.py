"""
Админ-панель: досье пользователя в личных сообщениях бота.

Безопасность (раздел 7 ТЗ):
  - Бот реагирует ТОЛЬКО на ADMIN_IDS в личных сообщениях.
  - Все остальные в ЛС — игнорируются без ответа.

Триггеры:
  1. Пересланное сообщение от проверяемого пользователя.
  2. Команда /check @username или /check <ID>.

Формат досье (раздел 7 ТЗ):
  👤 Пользователь: @username (ID: ...)
  📅 В чате с: ...
  🔗 Пришёл по ссылке: ...
  ✉️ Сообщений всего: ...
  🟢 Активность за 60 дней: ...
  ⏱ Первое сообщение: ...
  ⏱ Последняя активность: ...
  🛑 Попыток нарушений: ...
  ⚖️ Статус: Доверенный / Новичок / Спящий

Статус «Спящий» — много сообщений суммарно (>= TRUST_LIMIT),
но в окне свежести пусто. Ключевая целевая категория для модерации.

Подводные камни (раздел 8 ТЗ):
  - Приватный forward: ID отправителя недоступен → сообщить «не удалось определить».
  - /check @username ненадёжен (username меняется) → по возможности работать по ID.

# TODO (раздел 7 ТЗ): реализовать router с фильтром на ADMIN_IDS и ChatType.PRIVATE.
#   Обработчики: forward → извлечь user_id, /check → распарсить аргумент,
#   вызвать queries.get_user_profile, отформатировать и отправить досье.
"""

from __future__ import annotations

from aiogram import Router

router = Router()

# TODO (раздел 7): @router.message(F.chat.type == "private", F.forward_from)
#   — досье по пересланному сообщению.
# TODO (раздел 7): @router.message(Command("check"), F.chat.type == "private")
#   — досье по /check @username или /check ID.
# TODO: фильтр на ADMIN_IDS должен быть на уровне роутера или каждого хендлера.
